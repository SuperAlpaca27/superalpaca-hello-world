name: superalpaca-norev
description: |
  norev
version: "norev"
summary: norev
base: ubuntu@24.04
platforms:
  amd64:

parts:
  thing:
    source: bld:superalpaca-curl
    source-channel: curl/edge
    plugin: nil
